# 历史日期归档

该目录完整保留固定 Qualification 体系建立前的公开测试结果和场景清单。
其中的日期目录、旧案例名和旧报告链接不会继续扩展，也不计入新的固定
测试矩阵。

- `results/`：原按日期发布的黑盒结果。
- `scenarios/`：原测试活动清单。
- `TEST_MATRIX.md`：迁移前最后一次自动汇总。

历史失败结果仍有价值，但不能直接当作当前代码版本的 Qualification
结论。需要复验的场景应映射到根目录 `StarTrack/<signal>/` 的固定案例。
